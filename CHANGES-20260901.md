# CHANGES-20260901 — 认领材料改造 + 删生产管理 + 转直采无视BOM + 材料采购独立

峰子 + 用友顾问拍板，秘书拆任务派辅助（Claude Code/DeepSeek V4 flash vision-exp）实施、秘书逐项验收，已提交推送（Sales_Purchase）。

> 核心思路（峰子拍板）：**材料（原料）彻底跟销售单解耦——材料入库零关联销售单，只有出库（领料）时才把成本挂到销售单归集；成品才由销售订单驱动去采购。**

---

## 一、转委外：认领原料 → 领料（只改文案，保留供料方式机制）

**峰子拍板**：「认领原料 改为 领料，领的是原料库中的库存。」

- 前端 `OutsourceFromSales.vue`：按钮「认领原料」→「领料」、弹窗标题「认领原料（订单级）」→「领料（订单级）」、全部提示/报错文案同步「认领」→「领料」；页面顶部补一句「领料 = 从原料库出库，成本挂本销售单」
- 后端 `outsource.py`：`create_claims`/`delete_claim`/`list_claims`/`get_sales_to_outsource` 的 docstring 与报错文案「认领」→「领料」
- **机制不变**：仍是按仓库总数量 FIFO 扣原料库存、写原料出库流水（挂销售单号）、写 `os_claim_material`（记批次+unit_cost）；**订单级供料方式（己方提供/包工包料）保留**，只改文案
- **关键结论**：领料的成本归集机制（出库+挂销售单+记批次成本）**在 `/outsource/claims` 早已实现**，本次只做语义/文案统一，不是重构

---

## 二、材料采购完全独立（入库零关联销售单，出库才挂）

**峰子拍板**：「材料采购完全独立，入库的时候都不与销售订单产生关联，只有出库的时候产生关系。」

- 材料（原材料）采购：业务员**单独填采购订单** → 原料入库，**入库零关联销售单**（明细不挂 `sales_item_id`）
- 只有**领料（出库）**时，才把材料成本按 FIFO 挂到销售单归集
- **现状已满足**：手填采购订单材料行 → `to_material`（purchase.py:956-987，不挂销售）→ 原料入库收货（`stock_in.py receive`，unit_cost 取采购价），此链路本就与销售单无关，本次无需新建
- 配套：`purchase.py` 销售转采购接口删除 `menu:production:orders` 权限（不再涉及生产）

---

## 三、销售订单转采购：只保留贸易型 + 转直采无视BOM

**峰子拍板**：「用 A，也就是所有的原材料都是自己填采购订单，而成品才是销售订单驱动到采购订单的。」

- **列表过滤收紧**（purchase.py:612）：`list_sales_to_purchase` 从 `production_status.in_(["已通知入库","已通知外发"])` 改为 `== "已通知入库"` —— **委外型的材料行从此列表消失**（材料改走「手填采购订单→原料入库→领料」独立链路）
- **转直采无视 BOM**（purchase.py:551-565）：`_so_row_requirements` 改为**恒返回产品本身**（`material_id=None, product_id=si.product_id, need_qty=si.quantity`），不再按 BOM 展开材料行。🔴 这是"不误伤贸易型"的关键——只改列表过滤不够，贸易型产品若维护了 BOM，旧逻辑仍会误展开成材料行
- 状态校验同步收紧（purchase.py:728/806）：`get_sales_to_purchase`、`create_orders_from_sales` 只允许 `"已通知入库"`，防止列表隐藏后仍用 API 对已通知外发行下采购单
- `create_orders_from_sales` 的"不在 BOM 内不允许采购"拦截删除
- 前端 `PurchaseFromSales.vue`：删除「转外发」来源标签分支、说明文案删"按 BOM 展开"、弹窗文案改"采购成品（转直采，无视 BOM）"
- **AGENTS.md 铁律 2 同步改**：「严格按 BOM：有 BOM 的产品只能采购 BOM 内物料」→「BOM 只约束委外领料量：转直采按成品采购无视 BOM；委外领料按 BOM 算需领量」（否则下个打手会把改动当 bug 修回去）

---

## 四、删生产管理模块（公司无自产）

**峰子拍板**：「生产管理，我是不要的，咱们没有生产。」

- **删生产订单/生产工作台/生产详情**：前端删 `ProductionOrders.vue` / `ProductionWorkspace.vue` / `ProductionDetail.vue`；后端 `production.py` 重写为**仅保留批次追溯端点**（`/inventory/batch`、`/inventory/trace`），删除全部 `/productions*`、`/workspace` 端点；`schemas/production.py` 删；`schemas/__init__.py` 移除导出
- **销售订单页「转生产」入口删除**：`SalesOrders.vue` 删转生产按钮与 `handleProduce`；`business.js` 删 `salesApi.orders.reProduce`
- **AI 助手能力删除**：`ai_chat.py` 删 `issue_materials`、`production_receipt` 工具及 `TOOL_PERMS`/`AUDIT_TOOLS`/`TOOL_EXECUTORS` 对应项；`MatsuAssistant.vue` 删「生产·发料/入库」能力定义；`useBotChat.js`/`Dashboard.vue`/`BotChat.vue` 生产跳转/文案同步清理
- **权限码删除**：`menu:production:orders/workspace/invoices/receipts` 挂到 `DEPRECATED_PERMS` 走既有清理逻辑；`auth.py` PERMISSION_DEFS 生产管理仅保留「批次追溯」
- **保留**：`sales.py` 转直采/转外发里的 ProductionOrder 活跃单拦截 & `has_active_mo`（模型保留则逻辑可留，历史 MO 仍有效拦截）；`mo_*` 数据表全保留只断入口

---

## 五、加工费发票：删除页面/端点/权限码（模型保留）

**峰子拍板**：「加工费我不需要，加工费就变成委外订单来，委外订单要开票（财务留给朋友考虑）。」

- 前端删 `views/production/ProcessingInvoices.vue`；`business.js` 删 `processingInvoices` API
- 后端 `production.py` 删 `/processing-invoices*` 端点（1307-1397）；`schemas` 里 ProcessingInvoice schema 删；`menu:production:invoices` 权限码删（main.py + auth.py PERMISSION_DEFS 同步）
- **保留**：`ProcessingInvoice` 模型类、`mo_processing_invoice` 表、`foundation.py` 删除守卫引用（避免删供应商/产品时报 not defined）——**只断入口，保历史数据**
- 真实委外加工费记账入口 = **委外订单审核 → 生成应付账款**（outsource.py:214-241，`AccountsPayable.source_type="outsource"`）

> **待朋友考虑**（2026-09-01 峰子定，不做代码）：「委外订单要能开票」——详见 `docs/known-issues.md` 待定需求 001。若朋友后续要做，需另行开发（委外单开票、生成应付+进项发票）。

---

## 六、批次追溯：挪到库存管理菜单（挪不删）

**峰子拍板**：批次追溯是库存功能（读库存/流水），不是生产，挪到库存管理菜单。

- 前端 `router/index.js`：`production/inventory` 路由 path 改 `inventory/batch-trace`（name 保持 BatchInventory），perm 保留 `menu:production:batch`
- `BatchInventory.vue` 从 `views/production/` 挪至 `views/inventory/`
- `Layout.vue`：移除生产子菜单，库存子菜单新增「批次追溯」（排在库存查询旁）；pageTitle 增 `/inventory/batch-trace`、删 production 键
- 后端 `/production/inventory/*` 端点**不物理挪**（保留路径最稳）；`business.js` 的 `batch.query/trace` 后端路径 `/production/inventory/*` 第一轮不动
- **保留**：`menu:production:batch` 权限码（main.py:104 库存角色组已含，库管员无感切换）

---

## 七、验证（秘书亲自复验）

| 验证项 | 结果 |
| --- | --- |
| 前端 `vite build` | ✅ 通过（443ms，仅 chunk>500kB 告警）|
| 后端冷启动 | ✅ 无 ImportError，`Application startup complete` + `Uvicorn running` |
| 健康检查 | ✅ `status:ok` |
| 生产接口下线 | ✅ `/productions`/`/workspace`/`/processing-invoices` 全 404 |
| 批次追溯挪库存 | ✅ `/production/inventory/batch`、`/trace` 均 200（接口保留可用）|
| 委外领料接口 | ✅ `/outsource/claims` 200（机制保留，带销售明细行参数）|
| 转采购/转委外列表 | ✅ 200 可达 |
| `test_robust.py` 回归 | ✅ PASS=53、SKIP(已知 Bug)=1、FAIL=0 |

---

## 八、遗留 / 待办

1. **委外订单要开票**（财务方案留给朋友）——`docs/known-issues.md` 待定需求 001
2. **`views/production/` 空目录**：BatchInventory 已挪走，目录已空，未物理删除（不影响构建），后续可清理
3. **存量「已通知外发」行的在途材料采购单**：从转采购列表消失后，其采购完成状态不再展示（采购单本身照常入库，不受影响）；一次性盘点/迁移待后续
4. **已确认遗留 Bug（用户暂不修）**：超量入库（PO 订 100，二次入库 50 仍接受，累计 150）

---

## 关键决策记录（峰子拍板，防后续改回）

- 材料不挂销售单，出库（领料）才挂 → 成本在"离开仓库"时归集（记录级，不进毛利报表，峰子选 A）
- 转直采无视 BOM，恒买成品；BOM 只约束委外领料量
- 删生产管理（无自产），批次追溯挪库存、加工费发票删除
- 委外订单开票 → 留给朋友考虑财务方案，本轮不做
- 存量数据（mo_* 表、os_claim_material、加工费历史）**全部保留**，只断入口，保历史可回滚
