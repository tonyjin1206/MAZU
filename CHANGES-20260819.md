# CHANGES-20260819 — 转外发两条线 + 委外分工序 + 库存出库(大改造)

依据《ERP需求文档_V3.docx》5条需求开发,峰子多次拍板细化。全部改动经**端到端闭环测试**验证(两条链路真实跑通),测试数据已清理。

## 需求对照

| # | 需求 | 落地情况 |
|---|------|---------|
| 1 | 明细行变更可改数量+单价 | ✅ 变更弹窗加"新单价",后端重算订单头金额 |
| 2 | 点击反应慢、点多几次才有效 | ✅ 根因=23处@click绑定不存在函数(静默报错),已修复 |
| 3 | 转外发后先采购原料→原料入库→转委外认领材料;销售明细行删认领库存 | ✅ 两条线打通+认领迁移 |
| 4 | 库存管理加成品出库+原料出库 | ✅ 成品出库两步化(通知发货→库管出库)+新建原料出库 |
| 5 | 委外分工序委不同供应商,按BOM工艺流程选外协厂,思维导图界面 | ✅ 转委外页面大改造 |

## 明细改动

### 1. 销售订单明细行「变更」支持改单价
- **位置**: `frontend/src/views/sales/SalesOrders.vue`(变更弹窗加"新单价"输入框,默认当前单价,min 0.01);`backend/app/routers/sales.py`(update_order_item 接收 unit_price,校验>0,更新 unit_price_local=单价×汇率(round 6),重算 total_amount/excl_tax/tax_amount,调 _recalc_order_totals 重算订单头)
- **为啥**: 需求1。之前只能改数量/停售,改价要删单重建
- **验证**: 只改单价/数量+单价同改/单价0拦截/已转直采行拦截/已入库行拦截,接口实测5项全过,数据恢复原值

### 2. 销售明细行删除「认领库存/解绑」按钮
- **位置**: SalesOrders.vue(按钮/弹窗/函数全删,零残留)
- **为啥**: 需求3。认领功能迁移到「销售订单转委外」页面(委外时认领材料);后端 claim-batch/unclaim-batch 接口与 claimed_from_batch 字段保留(转委外复用其模式)

### 3. 全项目 23 处 @click 绑定修复(点击反应慢根因)
- **位置**: 采购订单/采购入库"⚙列设置"按钮(openOrderSettings→openOrderSettingsRaw)、12个页面表头右键"列排序..."菜单(openOrderDialog→openColumnSettings)、应收明细右键菜单、8个无列设置弹窗页面删除无效"⚙列设置"死按钮
- **为啥**: 需求2。Vue 点击绑定不存在的函数→静默 TypeError→界面无反应,用户只能反复点。后端接口实测毫秒级,非后端慢
- **验证**: 全量扫描178处按钮绑定,全部有定义;vite build 通过

### 4. 转采购列表支持「转外发」行(两条线)
- **位置**: `backend/app/routers/purchase.py`(sales-to-purchase 列表/详情/from-sales 校验放宽为 production_status in (已通知入库,已通知外发),列表返回 source 字段);`PurchaseFromSales.vue`(加"来源"列:转直采灰tag/转外发绿tag)
- **为啥**: 需求3。转外发=委外型,也要先买原料(采购线);转直采=贸易型保留

### 5. 委外单结构升级(工序+材料认领明细)
- **位置**: `backend/app/models/production.py`(os_order 加 process_id 列;新建 OutsourceMaterial 表 os_order_material: material_id/batch_no/quantity/unit_cost);models/__init__.py 注册
- **为啥**: 需求5。委外分工序,每张WO要记录工序和认领的材料(成本归集)

### 6. 转委外页面:从左到右层级关系面(两版迭代,第二版为峰子拍板)
- **第一版(被否)**:左右两块独立布局(左明细列表+右工序卡片横排)——峰子:"你弄得复杂了,不是要左右两块独立"
- **第二版(拍板版,当前)**:`OutsourceFromSales.vue` 重做,**上下主从 + 从左到右层级关系面**:
  - 上面=订单区域(转外发明细行列表:销售订单号/客户/产品编码/名称/规格/单位/数量/批次号/委外状态),点行选中
  - 下面=工序层级关系面:最左产品信息卡(产品/批次号/委外数量/已转),向右各工序按 seq 依次展开(节点间 ➜ 连线),一眼看清"这个产品有几道工序、顺序如何"
  - **穿刺进去**:点工序节点→就地展开配置面板(不弹窗):加工商下拉(默认工序默认供应商)+加工单价(默认工序单价,允许0)+委外数量(默认剩余量)+认领材料表格(全部BOM材料,弹窗选批次+数量,支持部分认领);`Set` 支持多道工序同时展开,再点收起,换行重置
  - 已生成工序=绿框只读(WO单号/状态/数量/加工商/单价/认领材料),提示去委外订单页调整
  - 底部:损耗%(默认10)+「生成委外订单」按钮,只提交已展开且选了加工商的工序,未配置的工序可分批追加
- **为啥**: 需求5+6。峰子要"从左到右的层级关系面,一目了然",不是两块拼的
- **验证**: vite build 通过;TestClient+接口实测:3道工序各配加工商/单价/认领材料→生成3张WO(10万/8万/5万)→os_order_material+库存扣减SQL核对全对;测试数据已清理

### 6.1 转委外页面第三版(2026-08-19 晚,峰子二次拍板:供料方式+硬校验)
- 峰子反馈:①产品信息卡删掉(重复)②工序不用点开直接排列③认领原料收敛成按钮④供料方式选择(己方提供/包工包料)⑤"转委外"和"生成委外订单"重复,留"转委外"
- 落地:`OutsourceFromSales.vue` 再改:
  - 删产品信息卡;工序节点直接排列(不点开),每节点直接显示:加工商下拉/加工单价/**总金额**/供料方式radio/认领原料按钮
  - 认领弹窗:按材料逐行认领,显示需求(委外数量×BOM用量×损耗%),选可用批次+数量
  - 删底部「生成委外订单」按钮;上面列表「转委外」=唯一入口,点前逐条校验所有工序(加工商/单价/供料方式;己方提供必须认领且数量够),不通过提示缺哪道
  - 已生成工序绿框只读;分批追加保留;换行重置
- 后端 `outsource.py`:os_order 加 supply_type 列(己方提供/包工包料,默认己方提供);from-sales-process 硬校验:
  - 己方提供:必须至少认领一种原料,每种已认领材料数量 ≥ 本次委外数量×BOM用量×(1+损耗%),不足400(需N已领M)
  - 包工包料:可不带材料(带了照常出库)
  - **🔴 校验需求基数修复(秘书验收发现)**:首版用整单销售数量(si.quantity)算需求,分批委外会卡死——改按**本次委外数量**(qty);且首版遍历全部BOM材料强制认领(工序只认领自己材料被拦),改为**只校验已认领的材料,但必须至少认领一种**
- **验证**: 秘书实测4场景全过(己方不认领→400/认领不足→400/认领够数→生成WO+出库/包工包料→无材料生成),SQL核对 supply_type/材料出库;测试数据已清理

### 6.2 供料方式:工序默认+材料级可覆盖(2026-08-19 深夜,峰子发现混合供料漏洞)
- 峰子发现问题:一道工序两种材料,一种包工包料一种己方提供,工序级供料方式表达不了(选己方提供会强制认领包工包料的材料,选包工包料则己方提供的没人管)。峰子拍板秘书方案:**工序默认+材料级可覆盖**
- 落地:`OutsourceMaterial`(os_order_material)加 supply_type 列(材料级:己方提供/包工包料,空=己方提供);`from-sales-process` 按材料级分流——「己方提供」材料认领量须 ≥ 本次委外数量×BOM用量×(1+损耗%),不足400(提示材料名+需N已领M);「包工包料」材料不校验、不写认领记录、不出库;全部材料包工包料=等价包工包料不强制认领
- 前端:认领弹窗每材料行带供料方式radio(默认=工序级),包工包料行灰字"由加工厂提供"批次/数量隐藏;提交只含己方提供材料
- **验证**: 秘书实测3场景全过(混合供料只扣己方材料/己方认领不足400/全包工包料无出库),SQL核对 supply_type;测试数据已清理

### 7. 🔴 多工序委外:只有末道工序生成成品待入库单(闭环修复)
- **位置**: `outsource.py` approve_order + 新增 _is_last_process()(按 ProductProcess.seq 最大判定末道;无工艺路线视为末道)
- **为啥**: 闭环测试发现:100件产品过3道工序,3张WO各入100=成品库存300,但订单只要100。峰子拍板:**只管理两端(出原料/入成品),中间工序只记加工成本**——非末道工序WO审核只生成AP,不生成待入库单
- **验证**: 审核3张WO→SQL核对仅末道1张待入库单+AP 3条;末道收货100→成品库存=100

### 8. 🔴 删除委外单,材料自动退回原批次(单据链闭环)
- **位置**: `outsource.py` DELETE /outsource/orders/{id}(删除前逐条退回 os_order_material:库存加回原批次+流水 material_out_return 正数+删明细,单事务)
- **为啥**: 闭环测试发现:材料出库扣了库存,WO删了材料不退=库存凭空消失。峰子铁律:能退回去才是闭环
- **验证**: 删WO→SQL核对材料1/2各+10000,退回流水2条

### 9. 新建「原料出库」页面+接口
- **位置**: `backend/app/routers/inventory.py`(POST /inventory/material-outs 手动出库,单号 MU-YYMMDD+2位,FIFO扣库存+流水;GET /inventory/material-outs 出库记录列表(手动+委外发料统一展示,来源按单号前缀 WO-区分);available-batches 支持 material_id 参数);`frontend/src/views/inventory/MaterialOuts.vue`(新建);菜单"原料出库"库存管理下(纯文字),路由 /inventory/material-outs,权限码 menu:inventory:material-outs
- **为啥**: 需求4。原料出库=手动按批次出库+展示转委外认领自动生成的出库
- **验证**: 手动出库500→MU-26081901+库存10000→9500(SQL核对);超量/0数量拦截;委外发料记录正确显示来源

### 10. 成品出库两步化(通知发货→库管出库)
- **位置**: `backend/app/routers/sales.py`(POST /sales/deliveries/notify 通知发货:只登记数量生成SD单(待出库),不选批次不扣库存;POST /sales/deliveries/{id}/issue 库管出库:选批次+数量,FIFO扣库存+流水,delivered_qty累计,状态部分出库/已出库;GET /sales/deliveries/outs 成品出库列表(带客户名+出库记录穿透));`frontend/src/views/sales/SalesDeliveries.vue`("发货"改"通知发货",弹窗只填数量;"已发货"列语义改"已出库";发货完成/撤销确认保留);`frontend/src/views/inventory/DeliveryOuts.vue`(新建,库管用);so_delivery 表 warehouse_id/batch_no 改可空+加 delivered_qty(三步重建表);菜单"成品出库"库存管理下,权限码 menu:inventory:delivery-outs
- **为啥**: 需求4。业务员通知发货→通知库管;库管收到单据按单发货→扣库存;判断订单发货是否完成=人工确认(保留)
- **验证**: 通知4000(不扣库存)→未出库退货被拒→部分出库1500→再出2500→已出库→超量拦截→退货500(库存加回+红字单),接口8项+SQL全过

### 11. 🔴 发货完成确认必须已出库(闭环修复)
- **位置**: `sales.py` delivery-confirm(确认分支开头校验 delivered_qty>0,否则400"该产品尚未出库,不能确认发货完成";撤销不受限)
- **为啥**: 闭环测试发现:没通知没出库也能点"发货完成",订单直接变已发货,数据失真
- **验证**: 未出库确认→400;出库后确认→200

### 12. 通知发货支持"生产中"订单状态
- **位置**: `sales.py` notify 校验放宽为 (已审,生产中,部分发货)
- **为啥**: 闭环测试发现:委外型订单转外发后状态为"生产中",成品入库后要发货被拦

### 13. 退货必须有已出库记录
- **位置**: `sales.py` create_delivery_return(开头校验 delivered_qty>0,否则400;批次必须是该行正常发货过的批次)
- **为啥**: 需求4。通知了没出的不能退,只有真发货(出库)才能退

## 测试:端到端闭环(峰子要求"要闭环")

### 主链路(委外型,产品有BOM+3道工序)
建销售订单100件→审核→转外发→转采购(按供应商拆3张PO,买a布料/b布料/五层纸箱各100)→审核→转原料库→原料入库收货(RM批次)→转委外(3工序卡片:胚布10元/染色8元/复合5元,各配加工商,认领材料自动出库)→生成3张WO→审核(仅末道工序生成待入库单,AP 1000+800+500)→末道收货100→成品库存=100→通知发货100→库管出库100→发货完成确认→退货50(库存+50,已发50)
**全链路每步✅,SQL核对库存/流水/状态/AP全对**

### 副链路(贸易型,产品无BOM)
建单50件→审核→转直采→转采购(无BOM展开=产品本身)→采购单→审核→转成品库→成品入库50(成本60/件)→明细已入库
**12项全✅**

### 数据清理
全部测试单据/库存/流水已按精确单号清理,基础档案(客户5/产品4/材料3/供应商4/工序3/BOM)保留,fd_bom_item 工序字段还原。备份: erp.db.bak_before_clean_0819_closedloop

## 备注
- 未 commit;所有改动在 Sales_Purchase 分支工作区
- 待峰子确认后提交(先本地commit→merge main→验证→推送前给看)
