# CHANGES-20260820 — 转委外订单级简化 + 全ERP退回补齐 + 库存查询改造(全天迭代)

峰子 08-20 凌晨至晚间连续反馈拍板,秘书拆任务派主力(codex-ds)实施、逐项验收,全部已提交推送(Sales_Purchase)。

## 一、转委外:订单级简化(第四版,推翻材料级)

**峰子拍板**:「认领材料不用管到每个供应商,只管批次就行;供料方式走在订单上就行;每一个加工商我就只管你单价和数量」
- **最终模型**:供料方式=订单级一个选择(只管整个订单发了多少料,算成本;不管给哪个供应商多少);加工费=按供应商分开结算(每工序一张WO);认领原料=订单级一个按钮,统一弹窗按仓库总数量认领全部BOM材料
- 落地:
  - `so_order_item` 加 `supply_type`;新建 `OsClaimMaterial`(os_claim_material 订单级认领记录);os_order_material 废弃
  - 新接口 `POST /outsource/claims`(认领量≥成品数量×BOM用量×(1+损耗%),扣库存+流水)
  - `from-sales-process` 简化(rows 只传加工商/单价/数量)
  - 工序卡片瘦身:只留加工商/单价/委外数量/总金额;订单级供料方式radio+认领按钮
  - 转采购页删「完成采购」;「采购需求」菜单/路由删除
- **验证**:8场景TestClient全过

## 二、转委外:工序卡片可自由删除(批次P)

**峰子拍板**:「BOM有3道工序,但有可能这次不委外这3个工序,工序可以自由删除」
- 未生成工序卡片加「删除」按钮(确认后移除,本次不生成该工序WO,不影响已认领原料)
- 已生成(绿框)不可删;底部「恢复全部工序」一键加回(按seq);待生成计数同步
- **验证**:构建通过,代码抽查

## 三、认领原料:按仓库总数量,不选批次(批次Q)

**峰子拍板**:「不能按入库批次来认领,要按仓库里总数量来领」
- `POST /outsource/claims` batch_no 改可选:不传=按仓库总数量认领(校验总可用≥认领量),FIFO跨批次自动扣(库存记录id升序),os_claim_material 按实际扣减批次分行
- 认领弹窗去批次下拉,显示「仓库总可用」,填数量即可;不足提示缺多少;已认领按材料合并显示
- **验证**:主力4场景(两批次3000+8000凑11000/超量400/单批次兼容/同批次累计)+秘书实测(认领成功/按批次分行/超量400)全过

## 四、全ERP退回补齐(峰子:整个ERP都要有退回,下游没退上游不能动)

### 1. 入库退回(原料入库/成品入库)
- 待入库单操作列加「退回」(POST /stock-in/{id}/cancel):退回后销售明细(已通知入库→未生产)/采购明细(解除去向)/委外单(已完工→已审核)全部解锁
- 已入库的「退回」改名「退数量」(红冲语义)

### 2. 原料出库退回
- 新接口 `POST /material-outs/{out_no}/return`(仅手动出库MU单;按单回补库存+红字流水 material_out_return+防重复;委外WO单拒绝并引导)
- 列表操作列「退回」按钮

### 3. 成品出库退回(库管红冲)
- 新接口 `POST /deliveries/{delivery_id}/issue-return`:仅该单出库过的批次可退;库存回补+红字流水 delivery_out_return;delivered_qty 回减;发货单状态回退(已出库→部分出库/待出库);明细行回减+确认撤销+订单状态回退(已发货→部分发货/已审)
- DeliveryOuts 加退回弹窗(批次=该单出库记录);客户退货(销售侧红字单)保留

### 4. 主力接手审查修复4问题(批次J)
- 原料退回回补口径(正数优先/全空新建,与销售退货一致)
- **🔴 既有bug:手动出库单号 generate_doc_no("MU") 查错字段(trans_no)→ 每天所有MU单号相同,第二单起退回误判/退错单 → 改按 source_doc_no 生成**
- 成品出库退回确认撤销条件(发生退回即撤销,与销售退货一致)
- 全部退回后订单状态回退(已发货/部分发货→已审)

### 5. 采购订单退回友好提示(批次K)
- delete_order 补 inv_stock_in 待入库单校验(带物料名):明细已转原料库/成品库时400提示「请先在入库页面退回待入库单」,不再500外键错误
- 前端确认弹窗追加提示

## 五、库存查询改造(三轮迭代,峰子逐步拍板)

### 1. 加数量列(批次L)
- 上面表格加「数量」列(批次库存总数,后端 balance 接口 quantity 已有)+合计;「订单数/已入库」移出默认显示

### 2. 弹窗明细改上下主从(批次M)
- 批次收货记录从弹窗改为**下方常驻明细区**(上=库存表,下=入库明细)
- 中间8px可拖动分界线(全ERP标准,高度记忆);明细表独立列设置

### 3. 按原料名称汇总(批次O,峰子:上面是总数量按原料名称汇总,下面才是每次入库明细)
- 上面表格前端按 material_id/product_id 聚合:**每种原料一行**(总数量/均价/总金额),批次行合并
- 新接口 `GET /inventory/material-receipts`:按物料查所有批次每次入库记录(入库日期/仓库/入库单号/本次数量/批次号)
- 点汇总行→下面显示该原料分几次入库的明细

## 六、列设置弹窗:拖拽排序+库存查询列精简(批次N)

**峰子拍板**:「列设置里太多不需要的列;可以不用上下箭头调整吗,可以用拖动吗」
- ColumnSettingsDialog(全ERP共用):↑↓箭头改 ⠿ 拖拽排序(原生HTML5,不引库,拖动高亮),提示「勾选=显示;拖动 ⠿ 调整顺序」
- 库存查询列精简:删「类型(显示数字)/订单数/已入库」3列;期间列(期初/入库/出库/期末)默认隐藏
- 所有页面的列设置弹窗同步升级

## 七、其他
- 路由:/purchase/requisitions 旧地址重定向到转采购(防白屏,采购需求菜单已删)
- 死代码清理:转采购 handleComplete 函数、business.js requisitions API
- 历史孤儿数据清理:po_receipt/po_receipt_item 引用不存在明细的记录、测试材料残留

## 验证总览
- 每个批次:主力TestClient真实场景验证+秘书抽查复核,全部通过
- vite build 全量通过(每次)
- 测试数据逐批清理,峰子数据完好(SO-26081901/02、材料库存各11000、产品-客户关联)
- 备份:erp.db.bak_* 齐全(各批次动库前)
- 08-20 全部提交已推送:0dce653→5457797→b8e13ac→74752b2→5eb1d4e→4cfca9b→17fff36→0d784a8
