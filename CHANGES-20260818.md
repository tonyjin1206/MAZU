# MAZU 改动说明书 — 2026-08-18

> 峰子要求（2026-08-18）：每改一处代码/数据，都要在此记录说明；上传 git 时本文件一并提交。
> 记录原则：改动位置（文件:行）→ 改了什么 → 为什么改 → 怎么验证的。

---

## 背景：SO-26081801-01 批次 "0 入库" bug

- **现象**：销售订单 SO-26081801 明细行（批次 SO-26081801-01）应收 1.0，委外单 WO-26081801 委外 1.1；待入库单（id=7）**已入数量 0.0 却显示「已入库」**，且库存台账、库存流水零记录、仓库为空；销售明细、委外单状态全变「已入库」。
- **根因**：后端 `backend/app/routers/stock_in.py` 的「确认完成」接口 `complete_stock_in`（~275-290 行）只校验了单据状态（待入库/部分入库），**没有校验已入数量 > 0**——货都没收就能盖章"已入库"，且不写库存、不写流水。前端「确认完成」按钮（StockIns.vue:60 / MaterialIns.vue:58）在「待入库」状态就显示，把陷阱摆到了用户手边。
- **连带问题**：0 入库后单子卡死——「退回」接口要求退回量 >0 且 ≤ 已入数量(0)，永远被拒；「退回待入库单」接口只认「待入库」状态，也被拒。只能改数据库纠正。

### 修复记录

**改动 1：脏数据还原（二蛋直改，2026-08-18 20:24）**

- 📍 `backend/data/erp.db`（备份：`data/erp.db.bak_20260818_before_0qty_fix`，先 `PRAGMA wal_checkpoint(TRUNCATE)` 落盘后复制）
- ✏️ 三条 UPDATE（峰子确认还原方向）：
  - `inv_stock_in` id=7：`status` 已入库 → **待入库**（received_qty=0 本就未收货）
  - `so_order_item` id=8：`production_status` 已入库 → **已通知外发**（委外线）
  - `os_order` id=2：`status` 已入库 → **已审核**（委外审核后生成待入库单，未收货前）
- 🤔 假"已入库"状态下库存/流水本就是空的，无需清理；还原后单据回到"待收货"的正常链路
- ✅ 还原后查库三行状态确认无误

**改动 2-4：代码修复（三蛋 codex-ds 实施，二蛋验收，2026-08-18 20:30）**

- 📍 `backend/app/routers/stock_in.py:287-288`（`complete_stock_in`）
- ✏️ 状态校验之后新增：`if (sin.received_qty or 0) <= 0: raise HTTPException(400, "还没有入库记录，不能确认完成")`
- 🤔 根因修复：0 收货也能盖章"已入库"（不写库存/流水、下游全绿、退不回去），加"必须已收过货"硬校验，前端绕不过
- ✅ 验收：重启后端后实测 `POST /stock-in/7/complete` → 400「还没有入库记录，不能确认完成」；拦截后单子状态/库存零变化

- 📍 `frontend/src/views/inventory/StockIns.vue:60` + `frontend/src/views/inventory/MaterialIns.vue:58`
- ✏️ 「确认完成」按钮 `v-if` 从 `'待入库' || '部分入库'` 改为仅 `'部分入库'`（待入库=一毛货没收，没东西可确认；「入库」「退回」按钮的 v-if 未动）
- 🤔 前后端同步：按钮只在有实际收货记录时出现，杜绝误点
- ✅ 验收：git diff 核实仅此三处改动、范围干净；`vite build` 通过（418ms/1705 模块）；后端接口实测拦截生效

---

## 新增 Bug 2：输入 0 被自动改成 0.01 入库 + RE 单号生成日期重复

- **现象**（峰子 2026-08-18 20:35 反馈）：入库弹窗输入 0，系统却入了 0.01（两次操作已入 0.02），用户要求"0 就是不能入库，只需要提醒，不能默认 0.01 入库"
- **根因 1**：前端 `el-input-number :min="0.01"`——输入 0 被组件自动拉回 0.01；`handleReceive` 虽有 `<=0` 校验，但看到的已是 0.01，拦不住
- **根因 2**（查数据时顺带发现）：`receive_stock_in` 内联的 receipt_no 生成用 `rsplit('-',1)[1]` 取序号，RE-26081802 短格式被整体当序号 → 生成 `RE-26081826081803`（日期段重复），与已修过的 generate_doc_no 同款坑，此文件内联实现漏修

### 修复记录

**改动 5：0.01 假入库脏数据清理（二蛋直改，2026-08-18 20:37）**

- 📍 `backend/data/erp.db`（备份：`data/erp.db.bak_20260818_before_clean_001`，先 WAL checkpoint 再复制）
- ✏️ 精确按 id 清理（峰子两次输入 0 产生的假入库）：
  - `DELETE inv_inventory WHERE id IN (4,5)`（两条 quantity=0.01）
  - `DELETE inv_transaction WHERE id IN (5,6)`（两条 ST 流水）
  - `UPDATE inv_stock_in id=7`：received_qty 0.02 → 0，status 部分入库 → 待入库
  - `UPDATE so_order_item id=8`：production_status 部分入库 → 已通知外发
- 🤔 0 数量入库本身不该发生，还原到"待收货"；按 id 精确圈定，未碰其他数据
- ✅ 清理后查库确认：待入库单/销售明细已还原，库存/流水零残留

**改动 6-8：代码修复（三蛋 codex-ds 实施，二蛋验收，2026-08-18 20:45）**

- 📍 `frontend/src/views/inventory/StockIns.vue:88` + `frontend/src/views/inventory/MaterialIns.vue:86`
- ✏️ 入库数量输入框 `el-input-number :min="0.01"` → `:min="0"`
- 🤔 根因修复：min=0.01 时输入 0 被组件自动拉回 0.01 提交；改 0 让 0 保留在框里，交给校验拦截
- ✅ 验收：git diff 核实；`vite build` 通过；接口实测 `receive qty=0` → 400「入库数量必须大于 0」

- 📍 `frontend/src/views/inventory/StockIns.vue:209` + `frontend/src/views/inventory/MaterialIns.vue:207`
- ✏️ 校验提示文案「请输入本次入库数量」→「入库数量必须大于 0」（校验逻辑未动）
- 🤔 用户明确输入了 0，提示"请输入"不准确；提醒要直白
- ✅ 验收：git diff 核实；build 通过

- 📍 `backend/app/routers/stock_in.py:163-173`（`receive_stock_in` 内联 receipt_no 生成）
- ✏️ 序号提取从 `rsplit('-',1)[1]` 改为 `max_no[len('RE-')+len(date_str):]`（去掉前缀+日期段后取数字，非纯数字兜底 seq=1），并抽出 `date_str` 变量复用
- 🤔 短格式单号（RE-26081802，日期序号间无横杠）被 rsplit 整体当序号 → 生成 RE-26081826081803 日期段重复；与 generate_doc_no 同款坑的内联漏网之鱼
- ✅ 验收：git diff 逻辑核对（RE-26081801 → [9:]="01" → 生成 RE-26081802 ✅）；后端重启后 health 正常；receive/complete 双拦截接口实测通过

---

## 全 ERP 统一「数量必须大于 0」校验（峰子 2026-08-18 要求）

- **需求**：所有数量操作（入库/出库/下单明细）数量 0 或负数一律不能操作
- **排查**：全量盘点后端 23 处数量取值点 + 前端 14 处 el-input-number
  - 已合规（不动）：成品/原料入库 receive、入库退回 return、销售发货、销售退货、认领库存 claim-batch、转采购 from-sales、转委外 from-sales、销售订单创建明细（后端均有 <=0 拦截）
  - **漏网 3 处**：①采购备货明细（purchase.py:244，无校验，默认值 1）②销售明细改数量（sales.py:1702，只拦负数不拦 0）③采购备货明细输入框（PurchaseOrders.vue:184，min=0 无提交校验）
- **SkillHub 排查**：搜索「数量 / positive number / input validation」三个关键词，无现成"数量>0"业务校验技能（只有通用 API 安全框架类），确认按项目现有模式自补

### 修复记录

**改动 9-15：代码修复（三蛋 codex-ds 实施，二蛋验收，2026-08-18 21:10）**

- 📍 `backend/app/routers/purchase.py:246`（update_order 备货明细循环）
- ✏️ `qty` 赋值后加 `if qty <= 0: raise HTTPException(400, "采购数量必须大于0")`
- 🤔 采购备货明细 0/负数可建的漏洞（此前完全无校验）
- ✅ 验收：git diff 核实

- 📍 `backend/app/routers/purchase.py:302-303`（create_order 创建明细循环，验收打回补的）
- ✏️ 加 `if item_data.quantity <= 0: raise HTTPException(400, "采购数量必须大于0")`
- 🤔 首轮只改了 update 漏了 create；实测创建 quantity=0 的采购单 → 400 拦截 ✅
- ✅ 验收：接口实测 400「采购数量必须大于0」，且确认未建单（PO 表无新增）

- 📍 `backend/app/routers/purchase.py:840`（create_receipt 采购入库收货，退役接口）
- ✏️ 明细循环开头加 `if item_data.quantity <= 0: raise HTTPException(400, "收货数量必须大于0")`（原代码无校验，三蛋读码确认后补）
- 🤔 模块已退役但接口保留，防绕过调用
- ✅ 验收：git diff 核实

- 📍 `backend/app/routers/sales.py:1702-1703`（销售明细改数量）
- ✏️ `if new_qty < 0` → `if new_qty <= 0`，文案「数量不能为负」→「数量必须大于0」
- 🤔 0 数量改进去的漏洞；后续「已入库不能变更」等校验未动
- ✅ 验收：接口实测 `PUT items/{id} quantity=0` → 400「数量必须大于0」

- 📍 `frontend/src/views/purchase/PurchaseOrders.vue:734/754`（备货采购创建/更新提交）
- ✏️ 提交前校验：任一明细 `!(parseFloat(item.quantity) > 0)` → `ElMessage.warning('明细数量必须大于 0')` 并 return
- 🤔 前端先拦，提升体验（后端已兜底）
- ✅ 验收：git diff 核实；vite build 通过

- 📍 `frontend/src/views/sales/SalesOrders.vue:620/851/907`（改数量弹窗 + 创建/编辑订单提交）
- ✏️ handleChange 非停售分支加 `if (!(changeForm.quantity > 0))` 提醒拦截；创建/编辑提交前校验明细行数量 > 0
- 🤔 同上，前后端双保险；停售分支不受影响
- ✅ 验收：git diff 核实；vite build 通过

- ✅ 数据完整性：全部接口实测拦截均未产生脏数据（PO 表无新增、SO 明细未变）

---

## 新需求：参数设置三按钮统一 + 加工工序 tab + BOM 工艺流程（峰子 2026-08-18 拍板）

**业务背景**：峰子公司=小型外贸公司，买布料委外加工（坯布/染色/底布复合等），无自有生产。需求：
1. **参数设置**里除仓库外所有 tab 统一「编辑/删除/停用」三按钮（原材料类别/通用参数用 el-switch 开关）；删除保护规则统一：**有引用禁删只能停用，无引用可删**（峰子拍板确认）
2. 参数设置新增**「加工工序」**tab：工序名称 + 默认加工单价（工艺流程带出可改），数据用现有 fd_process 表
3. **工序管理菜单并入 BOM 管理**：BOM 页下半部分两个页签「组成材料 / 工艺流程」，工艺流程引用参数设置的工序名称，支持添加工序/调顺序（上移下移）/删除
4. 后端产品工艺接口现成（GET/PUT/DELETE /products/{id}/processes），无需新建

**改动 16-17：批 1（三蛋 codex-ds 实施，二蛋验收，2026-08-18 21:40）**

- 📍 `frontend/src/views/foundation/SystemParams.vue`（+141 行）
- ✏️ ①材料类别/通用参数两个 tab：状态列 el-switch 改 el-tag（启用/停用），操作列加「停用·启用」按钮（与仓库 tab 三按钮统一，toggleActiveButton 包装现有 toggleActive）；②新增「加工工序」tab：GROUP_LABELS 加 process、loadGroup 分流 loadProcesses、表格（编码/工序名称/默认加工单价/状态/编辑·删除·停用·启用）、弹窗（工序名称必填 + 默认加工单价可空）、编码自动 GX+6 位流水（nextProcessCode，正则 /^GX(\d+)$/ 取最大+1 padStart(6)）
- 🤔 峰子拍板：参数设置除仓库外其他 tab 统一三按钮；加工工序放参数设置（数据用现有 fd_process 表，业务=委外加工工序如坯布/染色/底布复合）
- ✅ 验收：vite build 通过；接口实测创建工序 200（GX 编码+单价）、停用 200

- 📍 `backend/app/routers/foundation.py:89-95`（_process_delete_guard + register_crud(Process) 传 delete_guard）
- ✏️ 工序删除保护：查 fd_product_process（ProductProcess.process_id）引用，有引用 → 400「该工序已被产品工艺引用，不能删除，只能停用」
- 🤔 峰子规则：有引用禁删只能停用，无引用可删（全 ERP 统一规则）
- ✅ 验收：接口实测——无引用删除 200 ✅、有引用删除 400 ✅、有引用停用 200 ✅；测试数据已清理
- 🔴 **验收踩坑**：第一次实测"有引用删除 200"是假象——kill 命令与 vite build 捆绑被 Hermes 误判拦截，旧进程（PID 21331 旧代码）仍占 8788，新进程 bind 失败退出；确认新进程 PID 22709 后重测才通过。**kill 单独执行、重启后核对 PID**（skill 已有此坑，本次复现）

**改动 18-19：批 2 BOM 工艺流程（三蛋 codex-ds 实施，二蛋验收，2026-08-18 22:10）**

- 📍 `frontend/src/views/foundation/Bom.vue`（+130 行）
- ✏️ 右侧卡片改双页签「组成材料 / 工艺流程」（el-tabs，header 内嵌、无边框样式）；工艺流程表（工序编码/名称/默认加工单价/序号/操作列上移·下移·删除）；「添加工序」弹窗（el-select 选工序自动带出默认单价可改，processes-select 已过滤停用）；保存=本地重组数组 → `PUT /foundation/products/{id}/processes` 全量替换（后端现成接口，接收数组 process_id/seq/default_unit_price，删旧插新）
- 🤔 峰子拍板：工序管理并入 BOM，工艺流程按顺序引用参数设置的加工工序（坯布→染色→底布复合）；后端接口现成零新增
- ✅ 验收：vite build 通过；接口实测——创建 3 工序(GX 编码) → PUT 工艺(染色单价改 3.2) → GET 验证 seq 1/2/3 顺序+单价正确 → 清空清理，fd_process 零残留

- 📍 `frontend/src/components/Layout.vue`
- ✏️ 删「基础档案」下「工序管理」菜单项 + pageTitle 的 `/foundation/processes` 映射（路由/权限码/页面文件保留）
- 🤔 工序入口统一到 BOM 管理（数据并入），旧页面代码保留防引用
- ✅ 验收：`grep 工序管理 Layout.vue` 无匹配；vite build 通过

- 🔴 **验收踩坑 2**：测试脚本传 `code:''` 触发后端 500（register_crud(Process) 通用 CRUD 无 _next_code 自动生成逻辑，code 必填非空）——前端正常流程带 nextProcessCode() 生成的真实编码不受影响，但**工序编码自动生成目前靠前端算**（客户/供应商那种"后端强制生成"的待遇 Process 没有）。已记录，若后续要统一编码生成逻辑再处理

**今日改动汇总（2026-08-18）**：21 处改动 / 12 个文件 / 4 个问题闭环（0 入库、确认完成、单号重复、数量>0 全 ERP 统一）+ 2 个新功能（参数设置三按钮+加工工序 tab、BOM 工艺流程）

**改动 20-21：加工工序 tab 去掉默认加工单价（峰子 2026-08-18 22:25 纠偏，三蛋实施，二蛋验收）**

- 📍 `frontend/src/views/foundation/SystemParams.vue`
- ✏️ 加工工序表格删「默认加工单价」列；工序弹窗只留「工序名称」（删单价输入框）；processForm/openProcessCreate/openProcessEdit/handleProcessSave 的 unit_price 全删净
- 🤔 峰子原则：**参数设置只定义分类/名称（被引用的基础项），不涉及具体业务数据（单价）**；单价属于 BOM 工艺流程层
- ✅ 验收：grep unit_price SystemParams.vue 零残留；vite build 通过

- 📍 `frontend/src/views/foundation/Bom.vue`
- ✏️ 删 onProcessSelectChange 自动带出单价逻辑；添加工序弹窗单价输入框保留，label「默认加工单价」→「加工单价」，默认 0 用户手填
- 🤔 单价是 BOM 工艺流程的业务数据（每个产品每道工序的加工价），在添加工序时填、后期可改
- ✅ 验收：grep 确认函数已删、弹窗字段保留；vite build 通过

**改动 22：全 ERP 写死列批量补升降序排序（峰子 2026-08-18 22:35 反馈，三蛋实施，二蛋验收）**

- 📍 19 个文件 140 处（Dashboard 25 / ProductionDetail 18 / PurchaseOrders 16 / SalesOrders 14 / ProductionWorkspace 14 / Bom 9 / PurchaseFromSales 7 / SystemParams 6 / PurchaseReceipts 6 / MaterialIns 4 / StockIns 4 / InventoryManagement 4 / ProcessingInvoices 4 / TaxRefund 3 / OutsourceFromSales 2 / Collections/AR/AP/Payments 各 1）
- ✏️ Python 脚本批量给写死列（含 prop=、无 v-for、无 sortable 的 el-table-column）补 ` sortable`：行尾 `/>` → ` sortable />`、`>` → ` sortable>`；操作列/选择列（无 prop）天然排除；改动前后 `<el-table-column` 计数逐一比对确认无删行
- 🤔 峰子：参数设置加工工序无排序箭头 → "ERP 上其他适合的地方，都要有升降序排序"；写死列此前是 v-for 列批量补 sortable 时的漏网之鱼
- ✅ 验收：全量重扫剩余缺 sortable 写死列 = 0；diff 抽查改法正确；vite build 通过（428ms）
- 🔴 **派活坑**：三蛋首轮在 grep 抽查命令引号上崩（`no such file or directory: el-table-column` / unmatched quote）——指令改为"只用 python 脚本文件、不碰复杂 shell 命令"后一轮过。**派三蛋批量任务时避免让它写带引号/管道的 shell 命令**

**改动 23：基础档案默认升序（峰子 2026-08-18 22:50 确认：只改基础档案，订单/单据保持最新在最上）**

- 📍 `backend/app/routers/base_crud.py:24/93`
- ✏️ 通用 CRUD 列表默认排序 `model.id.desc()` → `model.id.asc()`（影响所有 register_crud 基础档案：工序/仓库/部门/员工/HS编码等，最早的在前）
- 🤔 峰子看到加工工序 GX000003→001 倒序别扭，要求基础档案默认升序
- ✅ 验收：重启后实测工序列表 GX000001→002→003 升序

- 📍 `backend/app/routers/foundation.py` 六个选择器接口（processes/warehouses/customers/suppliers/products/materials_select）
- ✏️ `X.id.desc()` → `X.code.asc()`（BOM 左侧产品列表、各下拉按编码升序显示）
- 🤔 选择器也是基础档案引用，统一升序
- ✅ 验收：实测 products-select FG000001→002→003 升序；**销售订单列表仍 SO-26081801（最新）在最前，订单类未受影响**

**改动 24：BOM 工艺流程加默认供应商 + 窗口上下布局（峰子 2026-08-18 23:10 要求，三蛋实施，二蛋验收）**

- 📍 后端：`models/foundation.py`（ProductProcess 加 `default_supplier_id` 列，**用 fd_supplier 不用旧 default_outsourcer_id**——系统铁律委外用 Supplier）、`schemas/foundation.py`（Item/Out 加 default_supplier_id，Out 加 supplier_name）、`routers/foundation.py`（GET 批量关联供应商名返回 supplier_name、PUT 写入 default_supplier_id）、SQLite `ALTER TABLE fd_product_process ADD COLUMN default_supplier_id INTEGER`
- 📍 前端 `Bom.vue`：右侧详情区 header 顶部加产品名称行（name（code））；工艺流程表加「默认供应商」列；添加工序弹窗 label「加工单价」→「默认加工单价」+ 新增「默认供应商」下拉（suppliers-select，code - name）
- 🤔 峰子：BOM 窗口分上下（上=产品名称，下=两个页签）；委外加工的默认供应商要能在工艺里定
- ✅ 验收：vite build 通过；实测 PUT 带 default_supplier_id → GET 返回 supplier_name「子供应商」正确；**测试后已还原峰子工艺数据（supplier_id 回 None，单价 10/0/0 原样）**

**改动 25：BOM 页面整体改上下布局（峰子 2026-08-18 23:20 纠正"你没改呀，是下面布局"，三蛋实施，二蛋验收）**

- 📍 `frontend/src/views/foundation/Bom.vue`（重构）
- ✏️ 删左侧产品列表卡片；改为上下布局：①顶部一行=「产品：壹成品（FG000001）」+「选择产品」按钮（弹窗选择器：780px 弹窗+编码/名称搜索+表格高亮选中，确定后加载 BOM 材料+工艺流程，重开弹窗自动高亮当前产品）；②下面 el-card=左上角两个页签「组成材料/工艺流程」+右上角按钮（新增材料/添加工序）+ 各自表格
- 🤔 峰子原始需求就是"上面产品名称，下面两个页面"；首版按 Bom.vue 原有左右结构改（左列表+右详情）被否，改成上下布局
- ✅ 验收：git diff 审查布局/产品选择弹窗/全部功能保留（材料增删改、工艺增删改排序、默认单价+默认供应商）；vite build 通过（exit 0）

**改动 26：BOM 顶部改全量产品列表（峰子 2026-08-18 23:40 纠正"要全部展现、滑轮拉动查看、不需要选择"，三蛋实施，二蛋验收）**

- 📍 `frontend/src/views/foundation/Bom.vue`
- ✏️ 顶部「产品名称+选择产品按钮」→ 全部产品列表卡片（搜索框+刷新按钮+表格 max-height 220px 滑轮滚动，编码/名称/规格/型号带 sortable，highlight-current-row 点行选中）；删产品选择弹窗及相关函数；打开页面自动选中第一行并加载材料+工艺
- 🤔 峰子试了弹窗觉得不方便看（一次只见一个产品），要求上面全部展现、滚动查看、点行切换
- ✅ 验收：git diff 审查（顶部列表/自动选中第一行 onProductSelect 逻辑）；vite build 通过（438ms）

**改动 27：BOM 上下区域加可拖分隔条（峰子 2026-08-18 23:50 要求"中间的距离要可以调整"，三蛋实施，二蛋验收）**

- 📍 `frontend/src/views/foundation/Bom.vue`
- ✏️ 上下卡片之间加 8px 分隔条 `.bom-divider`（@mousedown=startDrag，cursor row-resize、hover 高亮 #409eff）；topHeight ref（默认 240，localStorage「mazu_bom_top_height」持久化），拖动 clamp [140, innerHeight-320]，拖动期间全局 row-resize 光标+禁文本选中；产品表格 :max-height=topHeight 实时跟随
- 🤔 照 SalesOrders 主从联动页的上下分栏可拖动模式（项目既有惯例）
- ✅ 验收：git diff 审查（分隔条/topHeight/startDrag/clamp/持久化）；vite build 通过（375ms）

**改动 28：分隔条"拖不动"修复（峰子 2026-08-18 23:58 反馈，主力实施，二蛋验收）**

- 📍 `frontend/src/views/foundation/Bom.vue`
- ✏️ 产品表格 `:max-height="topHeight"` → `:height="tableHeight"`（`tableHeight = computed(() => Math.max(topHeight.value - 50, 100))`）
- 🤔 根因：max-height 只是上限，表格实际高度随内容走（3 行数据时约 130px），拖动改上限但表格高度不变 → 视觉"拖不动"；改固定高度后拖动立竿见影
- ✅ 验收：git diff 确认；vite build 通过（408ms）；分隔条 CSS 完整（8px/row-resize/hover 高亮，无遮挡）

**改动 29：合并 origin/main（2026-08-18 深夜，二蛋实施）**

- 📍 全仓库 34 个冲突文件
- ✏️ 分 4 批解决：①models 6 个（__init__.py 吞模型坑：补回 ProductCustomer/StockInOrder/OutsourceOrder/StockCheck；清 Outsourcer/OutsourceReceiptItem 死引用）②schemas/routers/main/ai_chat（采购需求回退+币种种子+审核权限装饰器+数量校验等两边逻辑合并）③api/Layout/router（main 的 hasPerm 权限体系保留+补 8 个独有权限码+菜单去图标+缩进 56px+朋友新菜单采购需求/盘点接入）④views 19 个整体取峰子版
- 🤔 朋友 main 有 v2.2-v2.5 大版本（权限体系/汇率/盘点/采购需求/操作留痕），与本地分叉；按大合并手册逐类处理，峰子拍板规则优先（审核不生成生产订单/产品类禁走采购入库），朋友新功能保留
- ✅ 验证：py_compile 全过；vite build 通过（462ms）；后端启动 health v2.5.0；登录+11 个关键接口全部 200（权限/工序/工艺/销售/委外/库存/参数/成品原料入库/盘点/采购需求/币种）；数据库巡检无缺列、朋友新表已建；git push 成功（8ac065b..831163d）
