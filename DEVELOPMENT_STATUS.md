# MAZU 外贸 ERP — 开发现状速览（2026-08-19）

> 给 codex / 任何接手开发者看的上下文。看完这份文档，你就知道这个项目在做什么、做到哪、下一步是什么。

## 1. 业务模型（最重要，先懂这个）

公司**无工厂**，只有两种业务模式：

1. **纯贸易**：买 A → 卖 A（采购产品直接转销售）
2. **委外加工**：买 ABC 材料 → 委托加工厂做成 D 产品 → 卖 D

对应两条业务线，**完全对称**（2026-08-19 更新：转外发=两条线同时走）：

```
销售订单明细行
 ├─ 点「转直采」(贸易型) → 采购管理 → 销售订单转采购页 → 生成采购订单（按 BOM 展开/按供应商拆单）→ 审核
 │    → 明细行「转成品库」→ 成品入库模块收货
 │    → 明细行「转原料库」→ 原料入库模块收货
 └─ 点「转外发」(委外型) → 两条线同时走：
      ├─ 采购线: 销售订单转采购页（来源=转外发）→ 按 BOM 买原料 → 转原料库 → 原料入库收货
      └─ 委外线: 销售订单转委外页（思维导图: 产品→工序卡片横向展开）→ 每道工序选加工商+加工单价+人工认领原料
           → 按工序拆多张委外订单（一工序一供应商）→ 审核 → 每张记加工费AP；只有末道工序生成成品待入库单
           → 成品入库模块收货（成品数量=产品数量，不是工序数×数量）
```

**发货（2026-08-19 两步化）**：
```
业务员「通知发货」(只填数量，不扣库存) → 库管在「成品出库」页按单选批次发货(扣库存) → 业务员判断「发货完成」(人工确认, 必须先有出库记录)
退货: 必须已有出库记录才能退（通知未出库不能退）
```

**原料出库（2026-08-19 新增）**：库存管理→原料出库页 = 手动出库(MU单号) + 转委外认领材料自动生成的出库记录(WO单号,来源=委外发料)

## 2. 核心铁律（全 ERP 强制）

1. **上游禁止下游改，退下游才解锁**：下游有单据时，上游不能变更/删除/退回。想改上游 → 先把下游单据全部退回（在对应单据页退回）→ 上游解锁。
   - 例：销售明细有采购单 → 不能变更；必须到「采购订单」页退回采购单后，销售明细才回"未生产"可改。
2. **状态颜色四档**（全 ERP 统一）：本环节完成=绿 / 进行中=橙 / 未开始=灰 / 终止=红。
3. **数量是参考，完成是人工点**：采购/委外的"完成"由业务员手动点（完成采购/完成委外），可"取消完成"再追加。
4. **损耗默认 10% 可改**：采购/委外数量上限 = 需求 × (1+损耗%)，超量拦截，要超就得改损耗比例。
5. **一步一步来**：销售订单只负责"推送"（转直采/转外发），单据在各自页面生成，不跳步。

## 3. 主流程状态机

### 销售订单明细行（production_status）
未生产 → 已通知入库(显示"转直采") / 已通知外发(显示"转外发") → 部分入库 → 已入库 / 已停售

### 转采购页状态（purchase_done 人工完成）
- 未采购（灰）→ 部分采购（橙，可追加）→ 已转采购订单（绿，达上限 需求×1.1）
- 手动「完成采购」→ 采购完成（绿）→ 「取消完成」可回
- 操作：采购 / 完成采购 / 取消完成 / 退回（仅无采购单时显示，有单去采购订单页退）

### 转委外页状态（outsource_done 人工完成）
同上对称：未转委外 / 部分转委外 / 已转委外订单 / 委外完成

### 采购订单状态
待审核（灰）→ 已审核（绿）→ 已关闭（红）。入库/开票/付款进度**不在主表**（财务模块看）。

### 委外订单状态
待确认 → 已审核（生成应付+待入库单）→ 已入库（收货完成）。已审核可「取消审核」退回待确认。

## 4. 菜单结构（2026-08-04 调整后）

- **采购管理**：销售订单转采购 → 采购订单 → 采购发票 → 应付账款 → 付款管理（~~采购入库已删除~~）
- **库存管理**：库存查询 → 收发存 → **成品入库** → **原料入库**（新增）→ 批次追溯
- **委外管理**：销售订单转委外 → 委外订单 → 加工费发票

## 5. 近期已完成的改造（git log 可见，均已推送）

- 销售订单明细行：转入库→**转直采**、转外发只推送；变更按钮按状态显隐（有下游单据就隐藏）
- 转采购/转委外：按**销售明细行**显示（不是整单）；状态口径"本节点完成=绿"；退回按钮（无单时撤销推送）
- 采购/委外完成改**人工判定** + 损耗 10% 可改 + 剩余量公式修正（需求×1.1-已购）
- 采购订单主表**瘦身**：删 7 个金额/财务列，只留 日期/订单号/销售订单号/供应商/明细/来源/状态/采购员/操作
- 委外订单主表加销售订单号列（与采购对称）
- **原料入库模块**：转原料库直接生成待入库单（material_id），RM 批次号、材料库存台账、收发存流水
- 转成品库去掉选择弹窗，点按钮直接生成待入库单
- 铁律漏洞封堵：转采购/转委外退回不再自动取消审核删单；委外补"取消审核"接口
- 全 ERP 状态颜色统一、基础档案弹窗两列布局、BOM 搜索排序等

## 6. 技术栈与环境

- 后端：FastAPI + SQLAlchemy + SQLite（`backend/data/erp.db`），启动：`cd backend && env -u PYTHONPATH PYTHONPATH=. venv/bin/python run.py`（8788）
- 前端：Vue3 + Vite + Element Plus，启动：`cd frontend && env -u PYTHONPATH npm run dev`（5173）
- 分支：`Sales_Purchase`，远程 `git@github.com:tonyjin1206/MAZU.git`
- 登录：admin / admin123（测试）
- 测试数据：业务表已清空重测，基础档案保留（客户4/供应商4/材料3/产品3/BOM2/仓库2）

## 7. 可能的下一步（未定，等峰子拍板）

- 委外完工/收货流程细测
- 采购发票/付款/应付与委外加工费发票联动
- 销售发货/报关/退税线
- 更多字段/页面的瘦身或调整
