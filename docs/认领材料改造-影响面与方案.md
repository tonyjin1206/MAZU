# 认领材料改造 — 影响面清单 + 改动方案

> 打手第二轮分析产出（2026-09-01，分支 Sales_Purchase）。本轮**只分析未改任何业务代码**。
> 所有关键代码位置均逐文件打开核实，行号为当前工作区实际行号。拿不准的标「待确认」，未凭空造文件/函数。

---

## 0. 核实结论先行（3 个与任务单预期不符的事实）

1. **「认领原料」后端机制其实已经是"出库挂销售单"**：`POST /outsource/claims`（outsource.py:692-829）当前就是按 FIFO 扣原料库存（跨批次）+ 写原料出库流水（`source_doc_no=销售单号`）+ 写 `os_claim_material`（挂 `sales_item_id`，记批次和 `unit_cost`）。**决策1/2 的实质机制已存在**，改造量集中在文案、入口和前置校验，不是重构。
2. **加工费发票并不是"可用的委外加工费开票功能"**：创建接口 `POST /production/processing-invoices`（production.py:1355-1367）当前**写死返回 400**「生产订单纯自产，无委外工序，无需开加工费发票」，`receipt-candidates` 恒为空（production.py:1387-1390）。该页面现状只能"看历史存量 + 删除"。真正的委外加工费走的是**委外订单审核 → 生成应付账款**（outsource.py:214-241，`AccountsPayable.source_type="outsource"`）。挪菜单可行，但挪过去它依然开不了新票（见待确认2）。
3. **销售订单页还有任务单没列到的「转生产」入口**：`SalesOrders.vue:121`（转生产按钮）+ `:558-562`（`handleProduce` → `salesApi.orders.reProduce`）；后端 `sales.py` 转直采/转外发接口里还有**两处活跃生产订单（ProductionOrder）拦截查询**（转直采 sales.py:604-611、转外发 sales.py:642-649）+ `has_active_mo`（sales.py:353-359、2713-2721）。删生产模块时这些"读它的"必须一起处理，否则按钮调不存在的接口/逻辑残留。

另：AGENTS.md 让先读的 `DEVELOPMENT_STATUS.md` 在仓库根**不存在**（当前工作区没有此文件），本次以代码为准。

---

## 1. 影响面清单

> "写它的"= 会改这份数据/状态的代码；"读它的"= 依赖它做展示或判断、改完必须跟上的代码。

### 改动点 A：认领原料 → 领料（决策1、2）

| # | 涉及文件 | 写它的 | 读它的 | 数据表/权限码 | 备注 |
|---|---------|--------|--------|--------------|------|
| A1 | `backend/app/routers/outsource.py` | `create_claims`(692-829 扣库存+流水+认领记录)、`delete_claim`(830-876 退回)、`list_claims`(657-691)、`get_sales_to_outsource` 内 claims 块(540-556)、`create_outsource_from_sales_process` 己方提供校验(929-945)、`delete_order` 退回旧委外单材料(278-337) | `list_sales_to_outsource`(373-437) | 写/读 `os_claim_material`、`WarehouseInventory`、`StockTransaction`；权限 `menu:outsource:from-sales/orders`（OUTSOURCE_WRITE_PERMS, :31） | 机制保留，主要改文案与（待确认1）供料方式前置 |
| A2 | `frontend/src/views/outsource/OutsourceFromSales.vue` | `confirmClaim`(确认认领)、`deleteClaimGroup`(删除退回)、`submitTransfer` 报错文案、`openClaim` 入口 | 订单级供料区(89-99)、认领按钮(96)、弹窗(173-212)、`groupedClaims`(已认领列表)、底部提示(167) | — | 全部「认领」字样改「领料」，语义提示同步改 |
| A3 | `frontend/src/api/business.js` | — | `outsourceApi.claims.list/create/remove`(181-184) | — | 接口路径不变则不改 |
| A4 | `frontend/src/views/outsource/OutsourceOrders.vue` | — | 删除提示读委外单材料 + `outsourceApi.orders` | 读 `os_order_material`（旧委外单级材料，存量） | 旧表只读兼容，不在本次改动范围 |
| A5 | `backend/app/routers/inventory.py` | — | `available-batches`(471-494，领料弹窗查可用批次) | 读 `WarehouseInventory` | 领料继续用，不改 |

### 改动点 B：材料采购独立 + 转采购删委外材料行（决策2、3、5）

| # | 涉及文件 | 写它的 | 读它的 | 数据表/权限码 | 备注 |
|---|---------|--------|--------|--------------|------|
| B1 | `backend/app/routers/purchase.py` | `list_sales_to_purchase` 过滤(622)、`create_orders_from_sales` 状态校验(816)、`get_sales_to_purchase` 状态校验(738)、`_so_row_requirements`(551-565)、`_so_purchase_status`(567-607)、行内 `row_status`(635-661)、`source` 三元(675)、`return_sales_to_purchase`(682-702) | 列表/详情/生成采购单三个接口互为读写 | 读 `so_order_item.production_status`、`fd_bom_item`；写/读 `po_order_item.sales_item_id`；权限 `menu:purchase:from-sales/orders` | B1 是"不误伤贸易型"的核心，见改动3/4/5 |
| B2 | `frontend/src/views/purchase/PurchaseFromSales.vue` | `submitPurchase` | `source` 列转外发标签(34-37,157)、页面说明(12-14)、弹窗文案、`openPurchase` | — | 列表只剩转直采后，来源列/标签/文案要清理 |
| B3 | `backend/app/routers/sales.py` | `notify_stock_in`(转直采,≈592-618)、`notify_outsource`(转外发,≈628-655)、`re-produce`(≈526-580) | 两接口内的 ProductionOrder 活跃单拦截(≈604-611, 642-649)、`has_active_mo`(353-359,2713-2721) | 写 `so_order_item.production_status`（已通知入库/已通知外发） | 转直采/转外发入口本身保留；生产拦截见改动7 |
| B4 | `frontend/src/views/sales/SalesOrders.vue` | `handleStockIn/handleOutsource`(540-556) | 转生产按钮(121)、`handleProduce`(558-566)、状态映射(525-536)、状态标签(227-233) | — | 转直采/转外发按钮保留；转生产删除（改动7） |
| B5 | `backend/app/routers/purchase.py`（材料独立链路，**已存在、无需新建**） | `create`(手填采购订单支持材料行,≈450-530)、`to_material`(956-987 生成待入库单，**不带 sales_item_id**) | 原料入库收货 `stock_in.py receive`(141-279，材料入库 unit_cost 取采购价) | 写 `po_order_item.receive_type`、`StockInOrder` | 决策2"入库零关联销售单"现状已满足 |
| B6 | `backend/app/routers/stock_in.py` | `_after_complete`(129-138 回写明细已入库)、成品入库成本 | `to_stock_in`(purchase.py:891-954) 生成待入库单 | 写 `so_order_item.production_status`、`WarehouseInventory` | 贸易型成品入库仍挂 sales_item_id，不动 |

### 改动点 C：删生产管理模块（决策4）

| # | 涉及文件 | 写它的 | 读它的 | 数据表/权限码 | 备注 |
|---|---------|--------|--------|--------------|------|
| C1 | `frontend/src/views/production/ProductionOrders.vue`、`ProductionWorkspace.vue`、`ProductionDetail.vue` | 整页删除（3 个文件） | — | — | 加工费发票 `ProcessingInvoices.vue`、批次追溯 `BatchInventory.vue` **不删**（挪菜单，见改动8/9） |
| C2 | `frontend/src/router/index.js` | 生产路由 39-41 删 3 条（orders/detail/workspace），**保留** invoices(42)/inventory(43) 两条（或改路径，见改动8/9） | 路由守卫读 `to.meta.perm`(88-98) | `menu:production:orders/workspace` | 注：40 行 detail 用 `menu:production:orders`，同码复用 |
| C3 | `frontend/src/components/Layout.vue` | 生产 sub-menu(96-105) 删「生产订单/生产工作台」两项+整个 sub-menu 拆解；pageTitle(260-263,279) | `hasPerm`(185) | `menu:production:*` | 注：router:38 注释说"菜单不显示"与 Layout 实际显示矛盾，以 Layout 为准 |
| C4 | `frontend/src/api/business.js` | `productionApi.productions.*`(114-137,144) 删、`salesApi.orders.reProduce`(64) 删 | `batch`(146-149) 保留、`processingInvoices`(138-143) 保留 | — | — |
| C5 | `backend/app/routers/production.py` | 删 `/productions*`、`/workspace` 端点(132-1305 绝大部分)；**保留** `/inventory/batch`+`/trace`(176-237)、`/processing-invoices*`(1307-1397) | 权限域常量(23-35)要收缩 | 写/读 `mo_production` 等 5 张表 | 端点清单已逐条核对（39 个 @router；132/1219 两个起始，176/1307 保留） |
| C6 | `backend/app/schemas/production.py` | 生产工单/领料 schema 随端点删除而失效 | 端点删后无读者 | — | 可整文件删或留空壳，建议删 |
| C7 | `backend/app/models/production.py`、`models/__init__.py`(irrelevant for deletion) | `ProductionOrder/Material/Process/Receipt` 4 个模型 | **sales.py 转直采/转外发仍 import ProductionOrder**(≈604,642)；`models/__init__.py` 导出 | 表 `mo_production/mo_production_material/mo_production_process/mo_receipt` | **第一轮建议模型和表全保留**，见待确认5/6 |
| C8 | `backend/app/main.py` | 权限码(47-52)、`DEPRECATED_PERMS`(74-88)、角色组(103-104,118-119)、路由注册(360-367) | 角色登录时下发权限 | `Permission/RolePermission` 表 | 删 `menu:production:orders/workspace/receipts`；invoices/batch 保留 |
| C8+ | `backend/app/routers/auth.py` | **`PERMISSION_DEFS`(162-208)** —— 角色管理页读取的权限目录（`GET /api/auth/permissions`），与 main.py 权限码**同步维护** | `frontend/src/views/system/Roles.vue:173` | 同上 | **任务单/既有分析漏列的第二处权限码写入方**，删码时须同步改这里 |
| C9 | `backend/app/routers/sales.py` + `SalesOrders.vue` | 同 B3/B4 | 转生产入口全链(按钮→api→端点) | — | 任务单漏列，本次补上 |
| C10 | `frontend/src/components/MatsuAssistant.vue:109` | — | `['menu:production:orders','生产·发料/入库']` 能力映射 | — | **既有分析漏列**：AI 助手能力列表，生产入口删后需同步移除/替换 |
| C11 | `backend/app/utils/ai_chat.py` | — | AI 工具 `issue_materials`(140,573)、`production_receipt`(152)、提示词(224-225,249) | — | **既有分析漏列**：AI 工具绑生产端点；生产入口删后需同步处理（或标待确认） |

### 改动点 D：加工费发票挪委外管理、批次追溯挪库存管理（决策4，**挪不删**）

| # | 涉及文件 | 写它的 | 读它的 | 数据表/权限码 | 备注 |
|---|---------|--------|--------|--------------|------|
| D1 | `frontend/src/components/Layout.vue` | 委外 sub-menu(85-92) 加「加工费发票」；库存 sub-menu(108-120) 加「批次追溯」 | `hasPerm` | `menu:production:invoices`、`menu:production:batch` | 权限码第一轮原样保留 |
| D2 | `frontend/src/router/index.js` | `production/invoices`(42)、`production/inventory`(43) 路径改挂 `/outsource/*`、`/inventory/*`（或仅挪菜单不改路径，见改动8/9） | 路由守卫 | 同上 | |
| D3 | `frontend/src/views/production/ProcessingInvoices.vue`、`BatchInventory.vue` | 文件**保留**（可挪目录 production/ → outsource/、inventory/） | `productionApi.processingInvoices`(business.js:138-143)、`batch.query/trace`(146-149，调 `/production/inventory/*`) | 读 `mo_processing_invoice`、`WarehouseInventory`、`StockTransaction` | 批次追溯数据源与生产无关，挪动安全 |
| D4 | `backend/app/main.py` | 库存角色组含 `menu:production:batch`(104)——库管员天然能看批次追溯 | | 同 D1 | 发票读权限域 `PRODUCTION_INVOICES_READ_PERMS`(production.py:30-33) 含 purchase:ap/invoices，财务也能看，不动 |
| D5 | `backend/app/routers/production.py` | 发票 4 端点(1307-1397)、批次 2 端点(176-237) **保留** | — | `mo_processing_invoice` | 是否把端点物理挪到 outsource.py/inventory.py：待确认4 |

### 回归验证影响面（改完必跑）

- 后端 tests：`test_state_machine.py`、`test_textile_flow.py`、`test_three_branch.py`、`test_rbac.py`、`test_inventory_v2.py`、`test_boundary.py`、`test_bot_agent.py`（以上均 grep 命中 production 端点；`test_bot_agent.py:201-205,261-263` 还断言"转生产"提示）、`test_dashboard_profit.py`（grep 命中做确认）
- e2e：`test_smoke_pages.py:37-40`（断言 /production/orders|workspace|invoices|inventory 可访问）、`test_rbac_menu.py`、`test_core_flow.py`
- scripts：`scripts/migrate_role_permissions.py:33-34,41`（production 集合 + inventory 含 `menu:production:batch`）、`scripts/migrate_permissions_sync.py:10,26,37`（production_manager 挂 receipts）
- 手工链路：销售审核→转外发→领料→转委外→审核→成品入库→发货；销售审核→转直采→采购→成品入库；手填材料采购→原料入库→领料

---

## 2. 改动方案（每改一处）

### 改动1：认领原料 → 领料（前端文案+语义提示）
- 文件/行：`OutsourceFromSales.vue` — 按钮(96)、弹窗标题(174)、说明(176,194-195)、已认领列表(98,201-207)、底部提示(167)、`submitTransfer` 报错文案(585)、`confirmClaim`/`deleteClaimGroup` 的 message；`outsource.py` — `create_claims`/`delete_claim`/`list_claims`/`get_sales_to_outsource` 的 docstring 与报错文案(692-876、488-530 多处)。
- 改前：「认领原料」「确认认领」「已认领 N 项材料」「请先认领原料」「材料退回原料库」等。
- 改后：统一「领料」「确认领料」「已领料 N 项材料」「请先领料」「材料退回原料库」；页面顶部说明补一句"领料=从原料库出库、成本挂本销售单"。
- 行为差异：**仅文案**，接口路径/参数/库存扣减逻辑不变。
- 风险/连带：列设置 localStorage 的 key 不含这些文案，无影响；测试断言若匹配旧文案会挂（跑 tests 验证）。注意 `MatsuAssistant.vue:109` 的"生产·发料/入库"是 AI 助手能力、`ai_chat.py` 的 `issue_materials` 是生产发料，**别把领料文案替换串到 AI 工具/生产发料上**（两套概念）。

### 改动2：领料前置校验按需调整（机制不动）
- 文件/行：`outsource.py` `create_claims`(706-724)、`create_outsource_from_sales_process`(929-945)
- 改前：领料强制要求订单级供料方式=「己方提供」（包工包料 400 拦截）；转委外时校验全部 BOM 材料领料量≥需求。
- 改后：机制保留（这是决策5"BOM 用来算委外领料量"的落地）；文案"认领"→"领料"。**是否保留供料方式概念见待确认1**——若峰子要彻底删掉"包工包料"，才需要动 `so_order_item.supply_type`、`os_order.supply_type`、`OutsourceMaterial.supply_type` 和 A2 整块 UI，改动面扩大。
- 风险/连带：领料不再依赖"先转外发"以外的新状态；`os_claim_material` 表结构不变。

### 改动3：转采购列表删委外材料行（列表过滤）
- 文件/行：`purchase.py:622`（`list_sales_to_purchase` 的 filter）
- 改前：`SalesOrderItem.production_status.in_(["已通知入库", "已通知外发"])` —— 转外发（委外）行也进转采购列表按 BOM 展开买材料。
- 改后：`SalesOrderItem.production_status == "已通知入库"` —— 只剩贸易型直采行；`:675` 的 `source` 三元恒为"转直采"（可简化掉）。
- 行为差异：已通知外发的行从此列表消失；委外材料改由"手填采购订单→原料入库→领料"链路采购（该链路已存在：purchase.py create 手填材料行 → `to_material`:956-987 不挂销售 → `stock_in.py receive` 收货）。
- 风险/连带：**存量**已通知外发、且已按 BOM 展开生成过材料采购单的行会从列表消失，其 `purchase_done` 完成标记无处展示（见待确认8）。

### 改动4：转采购"无视 BOM，纯买成品"（BOM 展开逻辑删除）
- 文件/行：`purchase.py:551-565`（`_so_row_requirements`）+ 四个调用方：`_so_purchase_status`(579)、`row_status`(650)、`get_sales_to_purchase`(746)、`create_orders_from_sales`(818-821)。
- 改前：有 BOM → 返回 BOM 材料需求清单；无 BOM → 返回产品本身。
- 改后：恒返回 `[{"material_id": None, "product_id": si.product_id, "need_qty": si.quantity}]`（即只买成品本身，需求量=销售数量）。**这是"不误伤贸易型"的关键**：只改列表过滤（改动3）不够，贸易型产品若维护了 BOM，旧逻辑仍会展开出材料行让采购员买料。
- 行为差异：转直采的采购需求从"BOM 展开（有 BOM 时）"变为"永远产品本身"；`create_orders_from_sales` 的"不在 BOM 内不允许采购"拦截(818-821)随之删除。
- 风险/连带：`_so_purchase_status`（销售单列表采购状态角标）与 `row_status`（转采购页状态）口径同步变为"按成品数量判定"；与铁律2"严格按 BOM"字面冲突，**AGENTS.md 铁律需同步改**（BOM 只约束委外领料，转直采无视 BOM）。

### 改动5：转采购详情/下单接口的状态校验同步收紧
- 文件/行：`purchase.py:738`（`get_sales_to_purchase`）、`purchase.py:816`（`create_orders_from_sales`）。
- 改前：两处均 `production_status not in ("已通知入库", "已通知外发")` 拒绝。
- 改后：只允许 `"已通知入库"`。防止列表隐藏后仍可用 API 对已通知外发行下采购单。
- 行为差异：已通知外发行对采购线彻底只读不可下单。
- 风险/连带：`return_sales_to_purchase`(682-702) 已只处理"已通知入库"，无需改；前端 `PurchaseFromSales.vue` 的"采购/退回/取消完成"按钮逻辑不用动。

### 改动6：转采购前端清理
- 文件/行：`PurchaseFromSales.vue:34-37`（转外发标签分支删）、`:12-14`（说明文案删"按 BOM 展开"）、弹窗文案改为"采购成品，数量上限=销售数量×(1+损耗)"、`:157`（source 列可留可删，建议删）。
- 改前：列表有"转直采/转外发"两来源、弹窗按 BOM 物料清单。
- 改后：单一来源；弹窗表格只有成品一行（`get_sales_to_purchase` 返回 rows 恒 1 行，模板自然兼容）。
- 风险/连带：无结构性改动。

### 改动7：删生产管理（订单/工作台/详情 + 销售页转生产入口）
- 文件/行：
  - 前端删文件：`views/production/ProductionOrders.vue`、`ProductionWorkspace.vue`、`ProductionDetail.vue`
  - `router/index.js:39-41`（orders/detail/workspace 三条路由删；38 行注释一并清理）、`Layout.vue:96-105`（生产 sub-menu 整块删，其中发票/批次两项挪到 D 组新位置）、`Layout.vue:260-263,279`（pageTitle 删生产键）、`business.js:114-137,144` 删 `productionApi.productions.*`、`business.js:64` 删 `salesApi.orders.reProduce`、`SalesOrders.vue:121,558-566` 删转生产按钮与 handleProduce
  - 后端 `production.py`：删 `/productions` 全系端点+`/workspace`(132-1305)，保留批次(176-237)与发票(1307-1397)；权限域常量(23-35)收缩为发票/批次两域；`schemas/production.py` 删
  - `main.py`：权限码删 `menu:production:orders/workspace/receipts`（挂到 `DEPRECATED_PERMS`:74 走既有清理逻辑，自动删角色关联）；角色组 `production`(103) 删空、`inventory`(104) 保留 `menu:production:batch`；「生产经理」角色(118-119)处理见待确认5
  - **`auth.py:162-208` `PERMISSION_DEFS`：同步删上面 3 个码**（否则角色管理页仍显示/下发已删码）
  - `sales.py`：转直采/转外发里的 ProductionOrder 活跃单拦截(≈604-611,642-649)与 `has_active_mo`(353-359,2713-2721) —— **模型若保留则逻辑可留**（存量 MO 数据仍有效拦截），见待确认6
  - `MatsuAssistant.vue:109`、`ai_chat.py`(140,152,224-225,249)：删/替换生产相关 AI 能力（见待确认4）
- 改前：生产订单/工作台/详情可建自产单、发料、完工入库；销售行可"转生产"。
- 改后：公司无自产，上述入口全部消失；批次追溯/加工费发票两功能挪菜单保留。
- 风险/连带：
  - `mo_*` 5 张表第一轮**不 drop**（存量历史数据、`sales.py` 引用、回滚余地），只断入口；
  - `stock_in.py`/`dashboard.py` 无生产订单强依赖（已核对：dashboard 毛利按发货出库算）；
  - e2e `test_rbac_menu`/`test_smoke_pages.py:37-40` 若断言生产菜单存在会挂，需同步改测试。

### 改动8：加工费发票 → 委外管理菜单（挪，不删）
- 文件/行：`Layout.vue` 委外 sub-menu(85-92) 加 `<el-menu-item index="/outsource/invoices" v-if="hasPerm('menu:production:invoices')">加工费发票</el-menu-item>`；`router/index.js:42` 路径 `production/invoices`→`outsource/invoices`（name 保持 `ProcessingInvoices`）；`Layout.vue` pageTitle 加 `'/outsource/invoices': '加工费发票'`；`ProcessingInvoices.vue` 文件可从 `views/production/` 移到 `views/outsource/`（import 路径相对调整）。后端端点/API 路径第一轮不动。
- 改前：菜单在生产管理下。
- 改后：菜单在委外管理下；权限码 `menu:production:invoices` 原样保留（有此码的角色无感切换）。
- 行为差异：**纯菜单归属变化，功能零变化**。特别说明：该页创建接口现状恒 400（见 0.2），挪过去后"委外加工费"实际的记账入口仍是「委外订单→审核→生成应付账款」。要不要把它改造成真能对委外单开票 = 待确认2，属新功能，不在本轮"挪菜单"范围。
- 风险/连带：给只有 `menu:outsource:*` 没有旧码的新角色授权时要用旧码名，名不副实（待确认3 换码可解）。

### 改动9：批次追溯 → 库存管理菜单（挪，不删）
- 文件/行：`Layout.vue` 库存 sub-menu(108-120) 加「批次追溯」（建议排在"库存查询"旁）；`router/index.js:43` 路径 `production/inventory`→`inventory/batch-trace`（避免与现有库存路由语义混淆）；`BatchInventory.vue` 移到 `views/inventory/`；`business.js:146-149` `batch.query/trace` 的后端路径 `/production/inventory/*` 第一轮不动。
- 改前：菜单在生产管理下，读权限 `PRODUCTION_BATCH_READ_PERMS=("menu:production:batch","menu:inventory")`（production.py:29）。
- 改后：菜单在库存管理下；权限码 `menu:production:batch` 保留（main.py:104 库存角色组已硬编码含它，库管员无感切换；且 `menu:inventory` 本就在读权限域里，双保险）。
- 行为差异：纯菜单归属变化。数据源是 `WarehouseInventory`+`StockTransaction`（production.py:176-237），与生产订单无关，删除生产端点不影响它。
- 风险/连带：若把端点物理挪到 `inventory.py`（待确认4），`business.js:146-149` 两行路径要同步——**典型的"读它的"漏改点，必须成对改**。

---

## 3. 待确认项

1. **领料是否仍保留"供料方式"前置**：现状领料/转委外都要求订单级 supply_type=「己方提供」（包工包料被拦）。决策1/2 没说供料方式去留。若彻底删"包工包料"概念，涉及 `so_order_item.supply_type`、`os_order.supply_type`、`OutsourceMaterial.supply_type` 三个字段+整块 UI，改动面翻倍——建议第一轮保留，只改文案。
2. **加工费发票挪过去之后要不要"能开新票"**：现状创建接口写死 400（纯自产无委外加工费），挪菜单后依然只能看历史+删。若峰子预期"委外加工费在这个页面开票"，那是要新开发的功能（按委外单开票、生成应付），需要单独立任务——按"挪菜单"验收的话它就是个历史查询页。
3. **权限码要不要改名**：`menu:production:invoices`、`menu:production:batch` 挪菜单后码名与模块不符。换新码（如 `menu:outsource:invoices`、`menu:inventory:batch`）需迁移 `RolePermission` 存量关联（可仿 `DEPRECATED_PERMS` 的"先删关联再删码"模式反向做）。第一轮建议保码，后续再清。
4. **批次追溯/发票后端端点要不要物理挪**（production.py → inventory.py/outsource.py）：不挪=后端留着 `/api/production/inventory/*`、`/api/production/processing-invoices/*` 路径，语义乱但零风险；挪=`business.js:139-143,146-149` 必须同步改。待峰子拍板。
5. **生产数据表与「生产经理」角色怎么处理**：`mo_production/mo_production_material/mo_production_process/mo_receipt` 4 张表有存量数据，`mo_processing_invoice` 也不能删。第一轮建议：模型和表全保留、只断入口；「生产经理」角色保留（权限收缩后自然只剩发票/批次+基础档案+库存）。是否 drop 表等观察一两个版本后再定。
6. **sales.py 里的活跃生产订单拦截删不删**：转直采/转外发接口各有一段查 `ProductionOrder` 拦截重复转单的代码（≈604-611、642-649）+ `has_active_mo`(353-359,2713-2721)。模型保留时这些代码能跑（历史 MO 仍拦截，无害）；若将来删模型则必须先删这几段。待确认第一轮是否顺手清掉（清掉更干净，留着更稳）。
7. **`os_claim_material` 存量认领数据的口径**：旧流程时代认领的数据已扣过库存、挂着 sales_item_id。领料语义切换后这些记录按新口径继续展示（=已领料），无需清洗——但若峰子希望"改造上线后历史认领全部退料重来"，要另出数据脚本。默认不动。
8. **存量"已通知外发"行的在途材料采购单**：这些行从转采购列表消失后，若之前已按 BOM 展开下过材料采购单（`po_order_item.sales_item_id` 指向它们），那些采购单本身不受影响（照常入库），但转采购页不再能看到这些行的采购完成状态。是否需要一次性盘点/迁移，待确认。

---

## 4. 我的独立判断

- **改造量比任务单语气暗示的小**：决策1/2 的机制（出库扣库存、FIFO、挂销售单、记成本）`/outsource/claims` 已经实现，真正的结构改动是改动3/4/5（转采购去 BOM 化）和改动7（删生产）。建议峰子确认：领料改造是否还有机制层面的新要求（比如领料要独立成单据、不再只是销售明细行上的记录），若有请补充说明，目前任务单文字读不出这层。
- **成本归集有个既有缺口，本轮改造填不上**：委外成品入库成本=0（`stock_in.py:194-210` 只取采购价，委外来源不取），驾驶舱毛利按"发货出库成本"算（dashboard.py:104-155），`os_claim_material.unit_cost` 记了但没人消费。也就是说"领料成本挂到销售单"目前是**记录级挂靠**，不进毛利报表。若峰子说"归集成本"是要在毛利里体现，那是另一个任务（委外成品入库成本=材料+加工费），这轮改动解决不了。
- **信息缺失提醒**：任务单说加工费发票"是委外加工费"，与代码事实不符（见 0.2）——它当前是自产产线的半废弃页面，委外加工费实际走委外订单审核生成应付。方案里我按"挪菜单+如实标注功能现状"处理，没有编造"挪过去就能开票"。
- **铁律文档要同步**：AGENTS.md 铁律2"严格按 BOM：有 BOM 的产品只能采购 BOM 内物料"与决策5"转直采无视 BOM"直接冲突，改造落地时铁律文案必须改成"BOM 只约束委外领料量，转直采按成品采购"，否则下一个打手会按旧铁律把改动4当 bug 修回去。
- **漏读方排查已做**（新增 2 处，任务单/既有分析未列）：
  - `已通知外发` 的全部读者：purchase.py:622/675/738/816、outsource.py:332/381/457/603/628/983、sales.py:622/649/2770/2825、SalesOrders.vue:231/525/533 —— 已穷举。
  - `menu:production:*` 的全部读者：router/index.js、Layout.vue、main.py:47-52/103-104、auth.py:162-208、production.py 权限域:23-35、foundation.py:70-71/77-78、purchase.py:48/50、sales.py:36/535、MatsuAssistant.vue:109、ai_chat.py:898-899 —— 已穷举。
  - `OsClaimMaterial` 的全部读写方：outsource.py 五处、models/production.py:157、前端 business.js:181-184 —— 已穷举。
  - `InventoryManagement.vue` 和 `sales.py` 的 `claim-batch`(≈657-690) 里的"认领"是**成品备货批次认领**（另一套功能，`claimed_from_batch` 字段/`batch_claim` 流水），与本次原料领料无关，不要混改。
